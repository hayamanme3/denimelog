<h1>id: <%= @denime.id %> の編集ページ</h1>

<%= form_for(@denime) do |f| %>
  <%= f.label :brand, 'brand' %>
  <%= f.text_field :brand %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to 'マイページに戻る', user_path(current_user) %>